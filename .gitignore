# Rust build artifacts
target/
.target-linux/
Cargo.lock.bak
*.rlib
*.rmeta

# pgrx local state
.pgrx/

# Compose: locally-built extension artifacts + PGDATA
compose/extensions/lib/*
!compose/extensions/lib/.gitkeep
compose/extensions/share/extension/*
!compose/extensions/share/extension/.gitkeep
compose/pg-data/*
!compose/pg-data/.gitkeep

# Env
.env
.env.local

# Fetched ontologies (manifest is checked in; payloads aren't)
fixtures/ontologies/

# Editor + OS
.vscode/
.idea/
*.swp
.DS_Store
.claude/

# Local reference symlinks (point outside the repo; per-workstation only)
ref-*/
ref-*

# Local WIP / private notes
_WIP/
RESPONSE*
SPEC.NATS*

# Generated PGXN source archive
pgrdf-*.zip
dist/pgxn/

# Large regression fixtures (wikidata truthy dumps) — kept local
fixtures/regression/*.nt

# Superpowers working artifacts (specs/plans) — local only
docs/superpowers/

# CKP v3.11 wave working documents — internal, never committed to this public repo.
# Covers CKP.*.VISION-REQUIREMENTS-STATEMENT.md and any SPEC.* drafts dropped here.
CK*.md
SPEC*.md
# ...but pgRDF's OWN published engineering specs are tracked, public, and unrelated to CKP.
!specs/SPEC.pgRDF.*.md
