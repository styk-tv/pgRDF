# 12-ask-true

W3C SPARQL 1.1 §16.2 — `ASK` returns true when the pattern has
at least one solution. The single `foaf:knows` triple makes the
match succeed.

Hand-computed expected output:

```jsonl
{"_ask": "true"}
```
