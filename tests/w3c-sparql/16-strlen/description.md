# 16-strlen

W3C SPARQL 1.1 §17.4.3.3 — `STRLEN(s)`. String length in
characters as a BIND projection.

Hand-computed expected output:

```jsonl
{"n": "Eve",     "len": "3"}
{"n": "Frances", "len": "7"}
{"n": "Gus",     "len": "3"}
{"n": "Heather", "len": "7"}
```
